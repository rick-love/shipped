<div id='form-container'>
	<h3> <%=@job.name%> </h3> 
	<div id='form'>
		<%= form_for Job.new do |f| %>  
			<p id='hide'><%= f.label :Job_id %>
			<%= f.text_field :job_id, :value => @job.id %></p>
			<%= f.label :Planes_id %>
			<%= f.select :plane_ids, @planes.collect {|x| [x.name, x.id]}, :multiple => true %>
			<%= f.submit %>
		<% end %>
	</div>

	<%= link_to "Back", jobs_path %><br>
</div>